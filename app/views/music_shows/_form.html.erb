<%= form_for(@music_show) do |f| %>
  <% if @music_show.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@music_show.errors.count, "error") %> prohibited this music_show from being saved:</h2>

      <ul>
      <% @music_show.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :band_name %><br>
    <%= f.text_field :band_name %>
  </div>
  <div class="field">
    <%= f.label :location %><br>
    <%= f.text_field :location %>
  </div>
  <div class="field">
    <%= f.label :date_of_show %><br>
    <%= f.date_select :date_of_show %>
  </div>
  <div class="field">
    <%= f.label :ticket_price %><br>
    <%= f.text_field :ticket_price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
